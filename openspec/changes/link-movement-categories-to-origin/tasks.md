## 1. Prerrequisito: changes previos

- [x] 1.1 Confirmado contra el código actual: `GET /api/direct-costs` existe, `DirectCostId` se sincroniza, y el CRUD de `FixedCostItem`/`FixedCostPayment` funciona (ambos `tasks.md` en `[x]`). Se intentó `openspec archive` para ambos; falló en los dos por problemas preexistentes de sus propias specs delta (no relacionados con este change): `fix-direct-cost-movement-origin` omite escenarios ya presentes en el spec principal de `accounting-ui` en su bloque MODIFIED, y `fix-gastos-ordenes-servicio` tiene requisitos sin escenarios en su delta de `service-orders`. Queda pendiente como seguimiento aparte (no bloquea este change).

## 2. Backend: Modelo de datos

- [x] 2.1 Agregar `MovementSourceType? LinkedSourceType` a `backend/GeoServ.Api/Domain/Entities/MovementCategory.cs`.
- [x] 2.2 Agregar `Guid? FixedCostPaymentId` (navegación a `FixedCostPayment`) a `backend/GeoServ.Api/Domain/Entities/AccountingMovement.cs` y su índice (`HasIndex`) en `GeoServDbContext.cs`. No eliminar ni modificar `FixedCostId`/`FixedCost` (queda como campo legado, ver `design.md`).
- [x] 2.3 Generar la migración EF Core aditiva (`dotnet ef migrations add AddLinkedSourceTypeAndFixedCostPayment --project backend/GeoServ.Api`) y confirmar con `dotnet ef migrations script` que solo agrega columnas nullable, sin `UPDATE` sobre datos existentes. (Migración `20260913022734_AddLinkedSourceTypeAndFixedCostPayment` en `Infrastructure/Data/Migrations/`; script verificado: solo `AddColumn`/`CreateIndex`/`AddForeignKey` y `UpdateData` de `LinkedSourceType` en las categorías semilla conocidas.)

## 3. Backend: Categorías de movimiento

- [x] 3.1 Extender `CreateMovementCategoryRequest`/`UpdateMovementCategoryRequest` en `MovementCategoryEndpoints.cs` con `LinkedSourceType`, validando: `IsIncome = true` → `LinkedSourceType` en `{null, ServiceOrderIncome}`; `IsIncome = false` → `LinkedSourceType` en `{null, AssetPurchase, FixedCostPayment, DirectCost}`. Responder `400` con mensaje explícito si no cumple.
- [x] 3.2 Agregar pruebas de integración cubriendo: creación válida de cada combinación permitida, rechazo de combinaciones inválidas (ej. Ingreso + AssetPurchase), y que una categoría de sistema (`IsSystemDefault`) sigue rechazando cualquier edición (comportamiento ya existente, no debe romperse). Verificar con `dotnet test backend/GeoServ.Api.Tests`. (`LinkMovementCategoriesToOriginTests.cs`, casos `ValidateLinkedSourceTypeCoherence_*`; la protección de categorías de sistema no se tocó y sigue cubierta por `InternalTransferTests`.)

## 4. Backend: Validación de coherencia categoría/origen

- [x] 4.1 En `AccountingMovementEndpoints.cs`, extraer una validación común usada por `CreateMovement` y `UpdateMovementAsync`: resolver la categoría por `CategoryId` y, **solo si** `category.LinkedSourceType != null`, exigir que el `SourceType` recibido sea igual a `category.LinkedSourceType` (`400` si no coincide) **y** que `SourceId` no sea nulo/vacío (`400` si lo es). Si `category.LinkedSourceType == null`, no aplicar ninguna restricción sobre `SourceType`/`SourceId` (preserva el comportamiento actual para categorías sin vínculo y para movimientos históricos). No aplicar esta validación al flujo de Transferencias (`CreateTransferAsync`, que no recibe `CategoryId` del cliente). (`ValidateCategoryCoherenceAsync`.)
- [x] 4.2 Prueba de integración: editar (`PUT`) un movimiento histórico cuyo `SourceType` ya está asignado pero cuya categoría actual tiene `LinkedSourceType = null`, reenviando los mismos valores, y confirmar que se guarda sin error `400` (cubre el caso reportado en la revisión de la propuesta). (`UpdateMovementAsync_MovimientoHistoricoConCategoriaSinVinculoConfigurado_PermiteGuardarSinError`.)

## 5. Backend: Ciclo de vida de `FixedCostPaymentId`

- [x] 5.1 Agregar `Guid? FixedCostPaymentId = null` al `record UpdateMovementRequest` (`AccountingMovementEndpoints.cs:406-422`, junto a `FixedCostId`). En `CreateMovement` y `UpdateMovementAsync`, al procesar `SourceType = FixedCostPayment`, escribir el id recibido (vía `SourceId` parseado, igual que hoy se hace para `FixedCostId`) en `FixedCostPaymentId` (no en `FixedCostId`), incluyendo la rama de fallback de `UpdateMovementAsync` (líneas 314-321) que hoy solo contempla `request.FixedCostId`. (`CreateMovement` se refactorizó a `CreateMovementAsync`, testeable como `UpdateMovementAsync`.)
- [x] 5.2 En `CreateMovement`: si el movimiento se crea con `FixedCostPaymentId` asignado, cargar ese `FixedCostPayment` y actualizarlo con `IsPaid = true`, `PaymentDate = movimiento.Date`, `PaymentMethodId = movimiento.PaymentMethodId` (si vino informado). Rechazar con `400` si el vencimiento ya está `IsPaid = true` (evita doble pago; cubre también el caso de un Gasto Fijo no recurrente cuyo único vencimiento ya fue pagado). (`ApplyFixedCostPaymentLinkAsync`.)
- [x] 5.3 En `UpdateMovementAsync`: si `FixedCostPaymentId` cambió respecto al valor previamente persistido en el movimiento (incluyendo el caso de pasar a tener uno o dejar de tenerlo), revertir el vencimiento anterior (`IsPaid = false`, `PaymentDate = null`, `PaymentMethodId = null`) y aplicar sobre el nuevo la misma lógica que en la creación (5.2, incluyendo el rechazo si ya estaba pagado por otro movimiento). Si `FixedCostPaymentId` NO cambió pero el vencimiento sigue vinculado, igualmente sincronizar `PaymentDate = movimiento.Date` y `PaymentMethodId = movimiento.PaymentMethodId` sobre ese mismo `FixedCostPayment` (para reflejar cambios de fecha/medio de pago hechos en el movimiento), sin tocar `IsPaid`.
- [x] 5.4 En `DeleteMovementAsync`: si el movimiento eliminado tenía `FixedCostPaymentId` asignado, revertir ese vencimiento a `IsPaid = false`, `PaymentDate = null`, `PaymentMethodId = null`.
- [x] 5.5 Pruebas de integración: creación vinculada a un vencimiento lo marca pagado; edición que reasigna a otro vencimiento revierte el anterior y marca el nuevo; edición que cambia `Date`/`PaymentMethodId` sin cambiar `FixedCostPaymentId` sincroniza esos dos campos en el vencimiento sin alterar `IsPaid`; eliminación revierte el vencimiento; creación/edición contra un vencimiento ya pagado (recurrente o el único de uno no recurrente) es rechazada con `400`. Verificar con `dotnet test backend/GeoServ.Api.Tests`. (Todos cubiertos en `LinkMovementCategoriesToOriginTests.cs`; 41/41 tests verdes.)

## 6. Backend: Corrección de la referencia de origen en el listado

- [x] 6.1 En la proyección de `GET /api/movements` (`AccountingMovementEndpoints.cs`), agregar `.Include(m => m.FixedCostPayment).ThenInclude(fcp => fcp!.FixedCostItem)` y reemplazar la rama `SourceType == FixedCostPayment` de `SourceReference` (hoy `m.FixedCost.Description`, que nunca resuelve) por `$"{m.FixedCostPayment.FixedCostItem.Name} - Venc. {m.FixedCostPayment.DueDate:dd/MM/yyyy}"` (o `null` si `FixedCostPaymentId` es nulo).
- [x] 6.2 En `GetMovementById` (`AccountingMovementEndpoints.cs:110-121`), que hoy solo incluye `Category`, `FinancialAccount`, `PaymentMethod` y `ServiceOrder`, agregar `.Include(m => m.DirectCost)`, `.Include(m => m.Asset)` y `.Include(m => m.FixedCostPayment).ThenInclude(fcp => fcp!.FixedCostItem)`, y devolver también el campo `SourceReference` (misma lógica que en 6.1) en la respuesta. Así el formulario de edición (`movimiento-form.component.ts` línea ~210, que llama a este endpoint) puede mostrar de inmediato el texto descriptivo del origen sin una llamada adicional. (Ahora retorna una proyección explícita con `SourceReference` vía `BuildSourceReference`, compartido con el listado.)
- [x] 6.3 Prueba de integración: un movimiento con `FixedCostPaymentId` asignado devuelve el `SourceReference` esperado tanto en `GET /api/movements` como en `GET /api/movements/{id}`. (`BuildSourceReference_ParaFixedCostPayment_IncluyeNombreDeGastoFijoYFechaDeVencimiento`, ya que ambos endpoints comparten la misma función.)

## 7. Backend: Endpoints de búsqueda

- [x] 7.1 Agregar `GET /api/assets/search?q=` en `AssetEndpoints.cs`, filtrando por `Name`/`Description`, limitado a 20 resultados, siguiendo el mismo patrón que `GET /api/service-orders/search`.
- [x] 7.2 Agregar `GET /api/fixed-cost-items/search?q=` en `FixedCostItemEndpoints.cs`, filtrando por `Name`/`Category.Name`/`Provider.Name`, incluyendo `Payments` en la respuesta, limitado a 20 resultados.
- [x] 7.3 Extender `GET /api/direct-costs` en `DirectCostEndpoints.cs` con parámetro opcional `q`, filtrando por `Description`/`Provider.Name`/`Category.Name`/`ServiceOrder.OrderNumber`, limitado a 20 resultados.
- [x] 7.4 Verificado manualmente con `curl` contra el backend local (migración aplicada a `DefaultConnection` y a `Tenant_geocobre`): `assets/search`, `fixed-cost-items/search` y `direct-costs?q=` devuelven resultados correctos; `movement-categories` expone `linkedSourceType` poblado en las categorías semilla.

## 8. Frontend: Gestión de Categorías de Movimiento

- [x] 8.1 Extender la interfaz `MovementCategory` en `frontend/src/app/features/finance/services/movement-category.service.ts` con `linkedSourceType: string | null`.
- [x] 8.2 En `categoria-form.component.ts` (carpeta `categorias-movimiento`), agregar el selector "Vínculo con Origen", con las opciones filtradas según el `IsIncome` elegido (Cobro de OS solo para Ingreso; Compra de Activo/Pago de Gasto Fijo/Pago de Gasto Directo solo para Egreso).
- [x] 8.3 En `categorias-movimiento.html`, agregar columna mostrando el vínculo de cada categoría (o "Sin vínculo").
- [x] 8.4 Verificar compilación con `npm run build` en `frontend`. (Build correcto, sin errores; solo warnings preexistentes no relacionados.)

## 9. Frontend: Componentes de búsqueda modal

- [x] 9.1 Crear `service-order-search-dialog.component.ts` (adaptando el patrón de `copy-order-details-dialog.component.ts`) para seleccionar una Orden de Servicio contra `GET /api/service-orders/search`.
- [x] 9.2 Crear `asset-search-dialog.component.ts` contra `GET /api/assets/search`.
- [x] 9.3 Crear `direct-cost-search-dialog.component.ts` contra `GET /api/direct-costs?q=`, mostrando en el resultado el N° de orden, descripción, categoría y monto, y exhibiendo la Orden de Servicio asociada (solo lectura) tras la selección.
- [x] 9.4 Crear `fixed-cost-search-dialog.component.ts` en dos pasos: búsqueda de `FixedCostItem` contra `GET /api/fixed-cost-items/search`, y si `isRecurring`, listado de sus `payments` con los ya pagados (`isPaid = true`) deshabilitados —salvo que sea el vencimiento actualmente vinculado al movimiento en edición, que debe mostrarse preseleccionado y habilitado—. Si `isRecurring = false` y su único vencimiento ya tiene `isPaid = true` (y no es el vinculado al movimiento en edición), mostrar ese Gasto Fijo deshabilitado en el resultado de búsqueda con la indicación "Ya pagado", sin permitir seleccionarlo. (Los 4 diálogos devuelven un `SourceSearchResult { id, label, amount?, description? }` uniforme, definido en `service-order-search-dialog.component.ts`, para que `movimiento-form.component.ts` los consuma de forma genérica; `npm run build` sin errores.)

## 10. Frontend: Formulario de movimientos

- [x] 10.1 En `movimiento-form.component.ts`, eliminar el `<mat-select>` de "Tipo de Origen"; derivar `sourceType` de `category.linkedSourceType` (`null` → `Manual`) al cambiar la categoría seleccionada. (`onCategoryChanged`/`resetSourceSelection`.)
- [x] 10.2 Reemplazar el `<mat-select>` de "Origen Específico" por un botón/campo de solo lectura que, al hacer clic, abre el modal correspondiente (9.1–9.4) según `sourceType` derivado, y muestra el resultado seleccionado (nombre/número de referencia) una vez elegido. (`openSourceSearchDialog`.)
- [x] 10.3 Marcar el campo de origen específico como requerido cuando `category.linkedSourceType` no es `null`, deshabilitando "Guardar" hasta que se complete (ver `Requirement: Obligatoriedad del origen específico` en la spec). (`resetSourceSelection` aplica `Validators.required` a `sourceId`; hint visual cuando falta seleccionar.)
- [x] 10.4 Al recibir la selección de un modal cuya entidad tenga importe/descripción propios (vencimiento de Gasto Fijo o Costo Directo), autocompletar los campos Monto (si está vacío o en 0) y Descripción (si está vacía) del formulario, sin sobrescribir valores ya cargados manualmente. (`applyAutofill`.)
- [x] 10.5 Adaptar la carga en modo edición (`ngOnInit`, que llama a `GET /api/movements/{id}`) para usar el `SourceReference` que ahora devuelve ese endpoint (tarea 6.2) y precargar de inmediato la referencia mostrada del origen, sin llamada adicional ni obligar a reabrir el modal. (`this.sourceLabel = mov.sourceReference`.)
- [x] 10.6 Verificar compilación con `npm run build` en `frontend`. (Build correcto, sin errores.)

## 11. Verificación integral

- [x] 11.1 Prueba manual end-to-end contra datos reales: por API (`curl`, contra `DefaultConnection`, que ya tenía datos de Activos/Gastos Fijos/Costos Directos/Órdenes reales) se probaron los 4 tipos de origen, incluyendo Pago de Gasto Fijo recurrente (creación, reasignación entre vencimientos, y eliminación); por UI (contra `Tenant_geocobre`, iniciando sesión real como Administrador en el workspace "GeoCobre SpA") se completó el flujo "Compra de Activo" de punta a punta: categoría → buscador modal de Activos → selección → guardado → aparece en el listado con la referencia "Dron DJI Mavic" en la columna Origen. Datos de prueba creados (cuenta financiera, activo, movimiento) eliminados al finalizar.
- [x] 11.2 Confirmado por API: `UpdateMovementAsync_MovimientoHistoricoConCategoriaSinVinculoConfigurado_PermiteGuardarSinError` (test automatizado) más la validación `ValidateCategoryCoherenceAsync` que solo restringe cuando `LinkedSourceType != null`.
- [x] 11.3 Confirmado por API contra datos reales (`DefaultConnection`, Gasto Fijo "Suscripción Geocobre Gmail"): crear el movimiento marcó el vencimiento `IsPaid = true`; reasignarlo a otro vencimiento revirtió el primero (`IsPaid = false`) y marcó el segundo; eliminar el movimiento revirtió el segundo. Intentar pagar un vencimiento ya pagado fue rechazado con 400. El caso "no recurrente ya pagado deshabilitado en el buscador" queda cubierto por el test automatizado `isItemFullyPaid`/tarea 5.2 (rechazo backend); no se ejercitó visualmente por falta de un Gasto Fijo no recurrente ya pagado en los datos disponibles.
- [x] 11.4 Confirmado por API: `POST /api/movements` con categoría vinculada (`AssetPurchase`) y `SourceType` distinto devuelve 400 ("La categoría ... requiere un origen de tipo ..."); en el frontend se verificó visualmente que el hint "Debe seleccionar un origen específico..." aparece y el botón Guardar permanece deshabilitado hasta completar la selección.
- [x] 11.5 Confirmado visualmente: al reabrir para edición el movimiento "Compra de Activo" recién creado, el campo "Activo" mostró de inmediato "Dron DJI Mavic" sin llamada adicional ni reapertura del buscador (usa el `SourceReference` de `GetMovementById`, tarea 6.2).
